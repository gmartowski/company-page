// children types: string, array, function

import React from 'react';

const cars = ["Saab", "Volvo", "BMW"];

export const ChildrenTypes = (props) => {
    return (
        <div>
            <div className="as functions">

            </div>
        </div>

    );
};

